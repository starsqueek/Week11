/**
 * Defines and implements (using adjacency lists) graphs.
 * Also defines, but does not implement, topologiocal sorts.
 * <b>Note:</b> Topological sorts are defined as a subclass of graphs.  This means that a topological sort object <i>is</i>
 * a graph.  It therefore doesn't need to be provided with a graph to sort.  It sorts <i>itself</i>.
 */
package graph;